# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local agent / IDE state
.claude/
.cursor/
.rocketride/

# Secrets — never commit
.env
.env.*
!.env.example

# Eval run artifacts
tests/results/

# Persisted reports (PHI — never commit)
data/reports/

# Unredacted ECG with handwritten patient name (PHI — never commit)
# The redacted version data/ecg_redacted.jpeg + provenance is safe to commit.
data/ecg.jpeg

# Local raw recordings (large; not part of the benchmark)
data/dump/

# Editor / OS
.DS_Store
.idea/
.vscode/
tests/bakeoff/

# Raw ECG / echo photographs — PHI (patient names, IDs, logbook). Never commit.
data/ecg/
